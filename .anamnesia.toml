# Anamnesia per-project configuration.
#
# Overrides ~/.anamnesia/config.toml for work inside this repository.
# Written by hand, or with: anamnesia config --project set <key> <value>

[identity]
project = "anamnesia"
